@* 
TODO please correct social media urls    
@using EternalBAND.Common
@using EternalBAND.DataAccess;
@using EternalBAND.Api.Options
@using Microsoft.Extensions.Options
@inject IOptions<SiteGeneralOptions> _siteSettings
@{
    var instagram = _siteSettings.Value.SiteInstagram;
    var facebook = _siteSettings.Value.SiteFacebook;
    var twitter = _siteSettings.Value.SiteTwitter;
    if (instagram != null || facebook != null || twitter != null)
    {
        <p class="text-white mt-3">Sosyal Medya Hesaplarımız</p>

        <ul class="footer-social-menu list-inline mb-0">
            @if (facebook != null)
            {
                <li class="list-inline-item">
                    <a href="@facebook">
                        <i class="uil uil-facebook-f"></i>
                    </a>
                </li>
            }
            @if (instagram != null)
            {
                <li class="list-inline-item">
                    <a href="@instagram">
                        <i class="uil uil-instagram"></i>
                    </a>
                </li>
            }
            @if (twitter != null)
            {
                <li class="list-inline-item">
                    <a href="@twitter">
                        <i class="uil uil-twitter"></i>
                    </a>
                </li>
            }

        </ul>
    }
} *@